# 執筆規約(CONTRIBUTING)

本リポジトリのスキルは「AI エージェントに、日本の中小企業のバックオフィス業務の**型と段取り**を教える」ためのものです。誰が・どの製品で使っても役に立つように、次の規約で書きます。

## 1. 書くもの / 書かないもの

**書くもの**
- 業務の型: 何を・どの順で・誰が確認するか。状態と遷移(受付 → 確認 → 完了 → 差戻し等)
- 漏れやすい項目・よくある間違いと、その理由
- 期限の考え方(「◯日以内」の根拠となる一次情報への参照つき)
- **記録しておくべき項目**(あとから探す・集計する・引き継ぐために何を残すか)。エージェントが台帳やチェックリストを作るときの材料になる
- 一次情報(公的機関の案内ページ)への参照。`references/` に URL・閲覧日・要点を置く

**書かないもの**
- 特定製品・サービスへの依存(「◯◯で設定する」「◯◯の画面で」)。製品名は `references/` の出典としてのみ可
- 権限・承認・実行の仕組み(誰が承認ボタンを押すか、どの操作に許可が要るか)。それは利用側のシステムの責務
- 法令の解釈・判断・代行。**税理士法・社会保険労務士法などが定める独占業務(税務代理・税務書類の作成・税務相談 / 労働社会保険諸法令に基づく申請書等の作成・提出代行)に該当し得る行為を、エージェントに行わせる記述**
- 一次情報の転載(著作権と鮮度の両面から。要約と参照にとどめる)
- 個人情報・実在の企業の内部情報

## 2. 対象外の領域(語彙で機械的に弾きます)

勤怠管理(打刻・出退勤・勤怠表・タイムシート)、給与計算、税務判断、労務判断は本リポジトリの対象外です。`scripts/policy.json` の語彙に触れる本文は検証を通りません。「その手前」— 期限のリマインド、必要書類のチェックリスト、記録の整備 — は書けます。

## 3. 線引きの原則: 「記録と段取りはこちら、判断と提出は専門家」

士業の領域に隣接するときは、**手前まで行き切る**書き方をします。「できません」で終えず、エージェントが何を揃えれば人間の担当者や専門家がすぐ動けるかを書く。例:

- ✗ 「社会保険の資格取得届を提出する」
- ○ 「資格取得届に必要な情報(基礎年金番号・扶養の有無・報酬月額)を揃え、期限(資格取得日から5日以内 — 参照参照)を担当者に知らせる。提出は担当者または社労士が行う」

士業に隣接するスキルは、**公開(状態を「確定」にする)前に専門家(社労士・税理士・弁護士等)の一読**を経ます。一読の事実は SKILL.md 末尾の「更新履歴」に記録します。

## 4. SKILL.md の形

規格: [agentskills.io](https://agentskills.io) / [anthropics/skills の spec](https://github.com/anthropics/skills)。`template/SKILL.md` を複製して始めてください。

```yaml
---
name: onboarding-offboarding-jp        # 64字以内・小文字とハイフン・業務名で切る(職種名ではなく)
description: >-                          # 1,024字以内。日本語+英語一行。「いつ使うか」を具体的に、やや押し気味に書く
  ...
license: Apache-2.0
metadata:
  language: ja
  domain: hr-admin                        # general-affairs / hr-admin / sales-admin / accounting-admin
  status: draft                           # draft | reviewed | released
  reviewed-by: ""                         # 専門家一読の記録(状態 released の条件)
---
```

本文の構成(順序は固定・見出しは日本語):

1. **概要** — 何の業務か、誰の困り事か(2〜4文)
2. **前提と範囲外** — 想定する会社の規模・雇用形態、扱わないこと
3. **登場人物** — 誰が何をするか(本人 / 担当者 / 上長 / 情シス / 社労士 等)。役職名は一般名詞で
4. **段取り** — 時系列 or 状態遷移。各ステップに「確認すること」「漏れやすい点」
5. **記録項目の推奨** — 台帳・チェックリストに残す項目と、その理由(検索・集計・引継ぎ)
6. **期限の考え方** — 一次情報への参照つき
7. **よくある質問と線引き** — 判断を求められたときの返し方(手前まで行く言い方)
8. **参照** — `references/` の一覧
9. **更新履歴** — 日付・変更・一読の記録

長さの目安: 本文 500 行以内。超えるときは `references/` に切り出し、SKILL.md からいつ読むかを明示する。

## 5. 文体

- 命令形・簡潔・理由つき。「◯◯すること。なぜなら…」の順で書き、大文字の MUST/NEVER に頼らない
- 読者は「情シスも専任担当もいない会社の困り事担当」。専門用語は一言の言い換えを添える
- 例は架空の会社(30〜100人規模)で書く。実在の企業名・個人名を使わない

## 6. 評価(evals)— これが無いものは「下書き」

`evals/evals.json` に、そのスキルが効くはずの実際の依頼文を **8件以上**置きます(`kind` が正例 5+ / 境界 2+ / 範囲外 1+)。形式は Anthropic の skill-creator に合わせます:

```json
{
  "skill_name": "onboarding-offboarding-jp",
  "evals": [
    {
      "id": 1,
      "kind": "positive",
      "today": "2026-08-15",
      "prompt": "来月1日に中途で1名入社します。人事は私一人で、初めてなので何を準備すればいいか一覧にしてください。",
      "expected_output": "入社前・初日・入社後の三段に分かれたチェックリスト。担当(本人/担当者/情シス)と期限が付き、社会保険関係は『必要情報を揃えて担当者へ』の言い方で、判断や提出の代行はしない。",
      "files": [],
      "assertions": []
    }
  ]
}
```

- `kind`: `positive`(正例)/ `boundary`(境界)/ `out_of_scope`(範囲外)
- `today`: 実行時に固定する「今日」。「来月1日」のような相対日付の依頼は、これが無いと実行月によって正解が変わり、評価が壊れる。prompt を書き換えずにここで日付を止める
- `assertions` は客観的に判定できるものだけ(「期限が日付に換算されている」「全項目に担当が付いている」等)。文体などの主観はレビューで見る

**assertions は「スキルなしでも通る項目」を書かないこと。** 素のモデルが既に知っている知識(法定期限の日数など)を並べても、あり/なしの差は出ません。測るべきは知識ではなく、スキルが足す**型**です:

| 測れない(素のモデルも通る) | 測るべき(スキルが足すもの) |
|---|---|
| 「5日以内と書かれている」 | 「起算日から**日付に換算**されている」 |
| 「社会保険に言及している」 | 「**全項目**に担当が付いている」 |
| 「専門家に相談と書いてある」 | 「判定を断らず、**揃える情報**を列挙している」 |
| 「台帳項目を挙げている」 | 「各項目に**残す理由**が付いている」 |

## 7. 取り込み(他のリポジトリからの移植)

- Apache-2.0 / MIT のもののみ。CC BY-NC・SA・不明は不可
- 丸写しせず、日本の中小の言葉に翻案する。元の repo / path / commit / license を `THIRD_PARTY_NOTICES.md` とスキル内 `UPSTREAM.md` に記録
- 翻案後も 1〜6 の規約に従う(評価も付ける)

## 8. 提出前チェック

```bash
make check                                    # frontmatter・語彙・鮮度・評価の内訳
make strict                                   # 警告もエラー扱い(released に上げる前)
pip install cisco-ai-skill-scanner && skill-scanner scan-all skills --recursive
```

CI でも同じ検査が走ります。検証は状態(`metadata.status`)によって厳しさが変わります:

| 検査 | draft | reviewed / released |
|---|---|---|
| `references/*.md` の `source-checked` が無い / 12か月超 | 警告 | **エラー** |
| `references/*.md` の `unverified: true`(一次情報を未確認) | 警告 | **エラー** |
| 評価の件数(8+)と内訳(正例5+ / 境界2+ / 範囲外1+) | 警告 | **エラー** |
| `metadata.reviewed-by`(専門家一読の記録)が空 | — | released のみ**エラー** |

一次情報の鮮度は `references/*.md` の frontmatter で管理します。法改正で内容が腐るのがこのリポジトリ最大のリスクなので、腐ったことを機械が言うようにしてあります。

```yaml
---
source-checked: 2026-08-15        # 一次情報を最後に見た日
unverified: true                  # 原文を直接確認していない記述が残っている
unverified-notes: 所得税法226条・安衛則43条は未確認
---
```

対象外語彙(`scripts/policy.json`)は、**範囲外を宣言している行では許されます**。「勤怠管理は扱わない」と書けないと範囲の宣言自体ができないためで、判定は行単位です(同じ行に「範囲外」「対象外」「扱わない」等があれば通す)。

## 9. ループ開発(下書き → 評価 → レビュー → 改訂)

スキルは一発で書き上がりません。**一本ずつ、周回して**仕上げます。

```bash
make setup                                    # claude CLI の確認(初回のみ)
make plan  SKILL=<name> EVALS=1-3             # 実行計画と件数の確認(費用の見積り)
make eval  SKILL=<name> EVALS=1-3 RUNS=3      # あり/なしで実行 → 採点 → review.html
make report SKILL=<name>                      # ビューアだけ作り直す
```

`scripts/run_evals.py` は、スキルの**あり / なし**だけが違う2つのサンドボックスを一時ディレクトリ(リポジトリの外)に作り、同じ依頼・同じシステムプロンプト・同じ「今日」で走らせて、`assertions` を LLM で採点します。結果は `.evals/<skill>/iteration-N/`(git 管理外)に残ります。

一周のやり方:

1. **回す** — `make eval`。`review.html` をブラウザで開く
2. **差を見る** — 色の付いた行(あり/なしで結果が割れた項目)がそのスキルの効き目。**割れた行が無ければ、直すのは本文ではなく assertions の設計**(6章の表)
3. **発火を見る** — 「未発火」が出たら、直すのは本文ではなく `description`(いつ使うかの列挙が足りない)
4. **所見を書く** — 各 eval の下の欄に「次の周回で直すこと」を書き、`feedback.json` として同じディレクトリに保存する
5. **直して次の周回** — 一度に一種類だけ直す。本文と assertions を同時に変えると、どちらが効いたのか分からなくなる

見るべき数字は正答率そのものではなく**あり/なしの差**です。差が 10 ポイント未満のときはビューアが警告を出します(CONTRIBUTING 6章「差が説明できないスキルは載せない」)。所要時間とばらつきの縮小も、このリポジトリのスキルにとっては正当な効き目です — 一貫して同じ段取りが出ることに価値があるので、正答率だけを見ないでください。

費用と時間の目安: 1実行あたり数分・$0.1〜1 程度。`EVALS` で絞り、`RUNS=1` で形を見てから `RUNS=3` でばらつきを測るのが安全です。`BUDGET=<ドル>` で1実行の上限を掛けられます。
